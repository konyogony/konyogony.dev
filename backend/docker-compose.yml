services:
    kony_backend:
        container_name: kony_backend
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "8080:8080"
        environment:
            - API_KEY=$API_KEY
        volumes:
            - .:/usr/src/app
        command: cargo run --release
